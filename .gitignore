.DS_Store

# nix build artifact (home-manager build)
/result

# Claude Code runtime state — only skills/, settings.json, and statusline.sh are tracked.
home/.claude/*
!home/.claude/skills/
!home/.claude/settings.json
!home/.claude/statusline.sh
